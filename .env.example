# Vercel Postgres Connection String
# Get this from your Vercel dashboard: Project Settings > Storage > Postgres
# For local development, you can use a local Postgres instance or Vercel's connection string
POSTGRES_URL=postgresql://user:password@host:port/database

# Optional: For migrations (non-pooling connection)
# POSTGRES_URL_NON_POOLING=postgresql://user:password@host:port/database

# Optional: Prisma-compatible URL (if needed)
# POSTGRES_PRISMA_URL=postgresql://user:password@host:port/database?pgbouncer=true

# Quiz Access Code (optional)
# If set, users must enter this code to access the quiz
# Set this in both .env.local (for local dev) and Vercel environment variables (for production)
# NEXT_PUBLIC_QUIZ_CODE=your-secret-code-here
