# Vercel KV (Redis) Connection
# Vercel automatically provides one of these when you create a KV database:
# 
# Option 1: REST API (older method)
# KV_REST_API_URL=https://your-kv-instance.vercel-storage.com
# KV_REST_API_TOKEN=your-kv-token-here
#
# Option 2: Direct Redis connection (newer method - what Vercel provides)
# REDIS_URL=redis://default:password@host:port
#
# The @vercel/kv package will automatically use whichever is available.
# For local development, use: vercel env pull .env.local

# Quiz Access Code (optional)
# If set, users must enter this code to access the quiz
# Set this in both .env.local (for local dev) and Vercel environment variables (for production)
# NEXT_PUBLIC_QUIZ_CODE=your-secret-code-here